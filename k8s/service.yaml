apiVersion: v1
kind: Service
metadata:
  name: starttech-backend-api
  labels:
    app: starttech-backend-api
spec:
  type: ClusterIP
  selector:
    app: starttech-backend-api
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
